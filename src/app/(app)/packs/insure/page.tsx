import { Metadata } from 'next';
import { InsureFlowPage } from '@/components/packs/insure/insure-page';

export const metadata: Metadata = { title: 'InsureFlow | SRP AI OS' };
export default function Page() { return <InsureFlowPage />; }
