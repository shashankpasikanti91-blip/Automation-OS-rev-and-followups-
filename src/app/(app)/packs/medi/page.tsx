import { Metadata } from 'next';
import { MediFlowPage } from '@/components/packs/medi/medi-page';

export const metadata: Metadata = { title: 'MediFlow | SRP AI OS' };
export default function Page() { return <MediFlowPage />; }
